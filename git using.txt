git add . 
//워킹 디렉토리 내 모든 파일을 추가.
//파일을 준비영역으로 옮긴다. 

git commit
//준비영역의 파일을 로컬저장소에 저장한다. 

git remote 
//로컬저장소와 원격저장소를 연결한다.

git push 
//원격 저장소에 저장한다. 

파일 업로드 순서
1. git add를 통해 파일을 준비영역으로 옮긴다
2. git commit을 통해 준비영역의 파일을 로컬저장소에 저장한다.
3. git push를 통해 로컬저장소에 있는 파일을 원격 저장소에 저장한다. 